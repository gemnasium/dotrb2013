= nav_bar do
  = menu_group do
    = menu_item "Home", root_path
    - if current_user
      = menu_item "Subscriptions", subscriptions_path
  = menu_group pull: :right do
    - if current_user
      = menu_item "Sign out", signout_path
    - else
      = menu_item "Sign in", "/auth/github"
